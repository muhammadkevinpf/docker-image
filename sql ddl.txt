-- pd_client."Client" definition

-- Drop table

-- DROP TABLE pd_client."Client";

CREATE TABLE pd_client."Client" (
	"ClientCd" varchar(50) NOT NULL,
	"ClientIDLifeAsia" varchar(255) NULL,
	"ClientType" varchar(255) NULL,
	"CustomerId" varchar(50) NULL,
	"Sex" varchar(20) NULL,
	"Firstname" varchar(255) NULL,
	"Lastname" varchar(255) NULL,
	"CitizenShip" varchar(255) NULL,
	"MaritalStatus" varchar(255) NULL,
	"ReligionCd" varchar(50) NULL,
	"DOB" timestamp NULL,
	"IDCardNo" varchar(255) NULL,
	"IDExpDate" timestamp NULL,
	"TaxStatus" varchar(255) NULL,
	"TaxID" varchar(255) NULL,
	"PICCorp" varchar(255) NULL,
	"CorpSIUPNo" varchar(255) NULL,
	"CorpSKPDNo" varchar(255) NULL,
	"RegularIncome" varchar(255) NULL,
	"OtherIncome" varchar(255) NULL,
	"IsDelete" bool NULL,
	"CreatedBy" varchar(255) NULL,
	"CreatedDate" timestamp NULL,
	"ModifiedBy" varchar(255) NULL,
	"ModifiedDate" timestamp NULL,
	"Email" varchar(100) NULL,
	CONSTRAINT pk_pd_client PRIMARY KEY ("ClientCd")
);

-- pd_report."SqsProductChoice" definition

-- Drop table

-- DROP TABLE pd_report."SqsProductChoice";

CREATE TABLE pd_report."SqsProductChoice" (
	"SqsProductChoiceCd" varchar(50) NOT NULL,
	"SqsSummaryCd" varchar(50) NULL,
	"ProductCd" varchar(50) NULL,
	"ProductDesc" varchar(255) NULL,
	"IsDelete" bool NOT NULL,
	"CreatedBy" varchar(255) NOT NULL,
	"CreatedDate" timestamp NOT NULL,
	"ModifiedBy" varchar(255) NOT NULL,
	"ModifiedDate" timestamp NOT NULL,
	CONSTRAINT pk_sqsproductchoice PRIMARY KEY ("SqsProductChoiceCd")
);


-- pd_report."SqsProductChoice" foreign keys

ALTER TABLE pd_report."SqsProductChoice" ADD CONSTRAINT "fk_productChoice_sqsSummary" FOREIGN KEY ("SqsSummaryCd") REFERENCES pd_report."SqsSummary"("SqsSummaryCd") ON UPDATE CASCADE ON DELETE CASCADE;

-- pd_report."SqsSummary" definition

-- Drop table

-- DROP TABLE pd_report."SqsSummary";

CREATE TABLE pd_report."SqsSummary" (
	"SqsSummaryCd" varchar(50) NOT NULL,
	"ProposalCd" varchar(50) NULL,
	"ExistInvestment" text NULL,
	"RecommendationReason" text NULL,
	"RpResult" varchar(50) NULL,
	"IsFundSuitability" bool NULL,
	"IsProductSuitability" bool NULL,
	"BillFreq" varchar(10) NULL,
	"BillFreqDesc" varchar(50) NULL,
	"IsAgreeRecommendation" bool NULL,
	"IsDelete" bool NOT NULL,
	"CreatedBy" varchar(255) NOT NULL,
	"CreatedDate" timestamp NOT NULL,
	"ModifiedBy" varchar(255) NOT NULL,
	"ModifiedDate" timestamp NOT NULL,
	"Premium" numeric(19,2) NULL,
	"ExistPolis" text NULL,
	CONSTRAINT pk_sqssummary PRIMARY KEY ("SqsSummaryCd")
);

-- wfstatus."MsChannelStatus" definition

-- Drop table

-- DROP TABLE wfstatus."MsChannelStatus";

CREATE TABLE wfstatus."MsChannelStatus" (
	"ChannelCd" varchar(50) NOT NULL,
	"ChannelId" varchar(50) NULL,
	"ChannelStatus" varchar(100) NULL,
	CONSTRAINT pk_channel PRIMARY KEY ("ChannelCd")
);

-- wfstatus."SpajStatus" definition

-- Drop table

-- DROP TABLE wfstatus."SpajStatus";

CREATE TABLE wfstatus."SpajStatus" (
	"SpajStatusCd" varchar(50) NOT NULL,
	"SpajNo" varchar(50) NULL,
	"ChannelId" varchar(50) NULL,
	"Date" timestamp NOT NULL,
	"WfCd" varchar(50) NULL,
	"ChannelCd" varchar(50) NOT NULL,
	CONSTRAINT pk_spaj_status PRIMARY KEY ("SpajStatusCd")
);


-- wfstatus."SpajStatus" foreign keys

ALTER TABLE wfstatus."SpajStatus" ADD CONSTRAINT fk_spaj_status_channel FOREIGN KEY ("ChannelCd") REFERENCES wfstatus."MsChannelStatus"("ChannelCd");
ALTER TABLE wfstatus."SpajStatus" ADD CONSTRAINT fk_spaj_status_wf FOREIGN KEY ("WfCd") REFERENCES wfstatus."MsWfStatus"("WfCd");

-- proposal."Proposal" definition

-- Drop table

-- DROP TABLE proposal."Proposal";

CREATE TABLE proposal."Proposal" (
	"ProposalCd" varchar(50) NOT NULL,
	"AgentCd" varchar(50) NULL,
	"QDFormCd" varchar(50) NULL,
	"QuotationCd" varchar(50) NULL,
	"ProposalType" varchar(255) NULL,
	"IsReplacementProposal" bool NULL,
	"ReplacedProposalNo" varchar(255) NULL,
	"SubmissionDate" date NULL,
	"ChannelCd" varchar(50) NULL,
	"CorrespondenceEmail" varchar(255) NULL,
	"CIFNumber" varchar(255) NULL,
	"CIFCode" varchar(255) NULL,
	"BankName" varchar(255) NULL,
	"BankAccHolder" varchar(255) NULL,
	"BankAccNo" varchar(255) NULL,
	"BankBranch" varchar(255) NULL,
	"BankCity" varchar(255) NULL,
	"Status" varchar(255) NULL,
	"IsEPolicy" bool NULL,
	"CorrPref" varchar(255) NULL,
	"TopUpPurpose" varchar(255) NULL,
	"IsDelete" bool NULL,
	"CreatedBy" varchar(255) NULL,
	"CreatedDate" timestamp NULL,
	"ModifiedBy" varchar(255) NULL,
	"ModifiedDate" timestamp NULL,
	"IsNf2f" bool NULL,
	"Campaign" varchar(50) NULL,
	"IsDropHs" bool NULL,
	"IsLampung" bool NULL,
	"IsWakafHidup" bool NULL,
	"IsWakafWasiat" bool NULL,
	"ProductCode" varchar(5) NULL,
	"LeadsChannel" varchar(10) NULL,
	"ProspectId" int4 NULL,
	"Version" varchar(20) NULL,
	"ApkVersion" varchar(10) NULL,
	CONSTRAINT pk_proposal PRIMARY KEY ("ProposalCd")
);

-- proposal."ProposalClient" definition

-- Drop table

-- DROP TABLE proposal."ProposalClient";

CREATE TABLE proposal."ProposalClient" (
	"ProposalClientCd" varchar(50) NOT NULL,
	"RoleCd" varchar(50) NULL,
	"ClientCd" varchar(50) NULL,
	"ProposalCd" varchar(50) NULL,
	"SmokersStatus" varchar(255) NULL,
	"SmokersStickperday" int4 NULL,
	"CompanyName" varchar(255) NULL,
	"CompanyType" varchar(255) NULL,
	"CompanyAddressCd" varchar(50) NULL,
	"WorkAddressCd" varchar(50) NULL,
	"isDifferentMailingAddress" bool NULL,
	"HomeAddressCd" varchar(50) NULL,
	"CorrAddressCd" varchar(50) NULL,
	"HasOtherInsurance" bool NULL,
	"IsDelete" bool NULL,
	"CreatedBy" varchar(255) NULL,
	"CreatedDate" timestamp NULL,
	"ModifiedBy" varchar(255) NULL,
	"ModifiedDate" timestamp NULL,
	CONSTRAINT pk_proposalclient PRIMARY KEY ("ProposalClientCd")
);
CREATE INDEX fki_proposalclient_proposal ON proposal."ProposalClient" USING btree ("ProposalCd");


-- proposal."ProposalClient" foreign keys

ALTER TABLE proposal."ProposalClient" ADD CONSTRAINT fk_proposalclient_proposal FOREIGN KEY ("ProposalCd") REFERENCES proposal."Proposal"("ProposalCd");